CREATE TABLE Article (
    title TEXT NOT NULL,
    description TEXT,
    date TEXT NOT NULL,
    imageUrl TEXT,
    content TEXT
);

insertArticle:
INSERT INTO Article(title, description, date, imageUrl, content)
VALUES(?, ?, ?, ?, ?);

selectAllArticles:
SELECT Article.*
FROM Article;

removeAllArticles:
DELETE FROM Article;